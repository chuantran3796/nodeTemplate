import express from "express";
// import controller from ".../controllers/DepartmentController";
// import JWTMiddleware from "../../../middleware/JWTMiddleware";
// import DepartmentController from "../controllers/DepartmentController";

const router = express.Router();

// router.get("/", DepartmentController.get);
// router.get("/:_id", DepartmentController.getOne);
// router.post("/", JWTMiddleware, DepartmentController.post);
// router.put("/:_id", JWTMiddleware, DepartmentController.put);
// router.delete("/:_id", JWTMiddleware, DepartmentController.delete);

export default router;
