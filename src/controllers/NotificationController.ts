import { NextFunction, Request, Response } from "express";

export default {
  get: async (req: any, res: Response, next: NextFunction) => {},
  getOne: async (req: any, res: Response, next: NextFunction) => {},
};
